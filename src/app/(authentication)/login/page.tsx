import { LoginCard } from "./_components/login-card";

const Page = () => {
    // const session = getSession()
    // if(session?.role) {
    //   redirect(`/${session?.role}`)
    // }
    return (<LoginCard/>);
}
 
export default Page;